name: Deploy GitHub Pages

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Pages is served from main branch root
        run: echo "GitHub Pages is configured to serve directly from the main branch root."
